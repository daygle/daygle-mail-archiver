# Development-only dependencies (not installed in production images).
# Runtime dependencies are pinned in api/requirements.txt and worker/requirements.txt.
-r api/requirements.txt
pytest==9.1.1
ruff==0.16.1
pip-audit==2.10.1
