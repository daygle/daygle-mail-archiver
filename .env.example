# ============================
# Daygle Mail Archiver - .env
# ============================

# Database connection string
DB_DSN=postgres://daygle_mail_archiver:change_me@db:5432/daygle_mail_archiver

# Directory where .eml files are stored
STORAGE_DIR=/data/mail

# API session secret (use a long random string)
SESSION_SECRET=3f2c1e9b0d4a8f7c2b1e9d4c7a3f8b2d

# IMAP password encryption key (Fernet, base64-encoded)
# Generate with:
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
IMAP_PASSWORD_KEY=Zk1qV0p2bG5xQ0t6d1h4R2p5b0p3c2t4bG5xQ0t6d1g=
